<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Book List</title>
</head>
<body>
  <% arrayBookItems.forEach(bookItem => { %>
    <% if(bookItem !== null) { %>
      <section>
        <img src=<%= bookItem.img_url %>>
        <h1><%= bookItem.title %></h1>
        <h2><%= bookItem.authors %></h2>
        <p><%= bookItem.description %></p>
      </section>
      <!--  include('/books/detail', { bookItem : bookItem }); -->
    <% } %>
  <% }) %>
</body>
</html>